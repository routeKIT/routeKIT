package map;

import profiles.VehicleType;

/**
 * Repräsentiert eine Beschränkung der Benutzung durch einen bestimmten
 * Fahrzeugtyp.
 */
public class VehicleTypeRestriction {
	/**
	 * (statisch) Gibt eine Instanz dieser Klasse für den angegebenen
	 * Fahrzeugtyp zurück.
	 * 
	 * @param type
	 *            Der Fahrzeugtyp.
	 * @return
	 */
	public Restriction getInstance(VehicleType type) {
		return null;
	}
}
