package precalculation;
/**
 * Stellt eine Kante im Straßengraphen dar. Dies ist nur eine vom
 * {@link OSMParser} verwendete Zwischendarstellung.
 */
public class MapEdge {
	/**
	 * Konstruktor: Erzeugt ein neues Objekt mit den angegebenen Attributen.
	 * 
	 * @param target
	 *            Die ID des Zielknotens.
	 * @param way
	 *            Der zur Kante gehörige OSM-Weg.
	 */
	public MapEdge(int target, OSMWay way) {
	}
}
