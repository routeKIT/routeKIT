package edu.kit.pse.ws2013.routekit.util;

/**
 * Beschreibt einen Punkt auf der Kante.
 */
public class PointOnEdge {
	/**
	 * Konstruktor: Erstellt ein neues Objekt aus den gegebenen Attributen.
	 * 
	 * @param edge
	 *            Die Kante.
	 * @param position
	 *            Der Wert für das Attribut {@code position}.
	 */
	public PointOnEdge(int edge, float position) {
	}
}
